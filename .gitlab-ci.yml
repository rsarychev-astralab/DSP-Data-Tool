stages:
  - test

pytest:
  stage: test
  image: python:3.13-slim
  before_script:
    - pip install -r requirements-dev.txt
  script:
    - pytest -q
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH
